{
  "name": "gulp-connect-proxy",
  "version": "0.2.0",
  "description": "A simple proxy middleware for gulp-connect",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Hoage",
  "license": "ISC",
  "dependencies": {
    "extend": "^2.0.0"
  }
}
